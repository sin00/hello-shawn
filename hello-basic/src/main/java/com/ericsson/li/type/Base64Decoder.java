package com.ericsson.li.type;

import java.util.Base64;

//import org.springframework.util.StringUtils;

public class Base64Decoder {/*
	public static boolean GetValueAsBool(String b64Value) {
		boolean result = false;
		if (!StringUtils.isEmpty(b64Value)) {
			byte[] values = Base64.getDecoder().decode(b64Value);
			result = BitConverter.ToBoolean(values, 0);
		}
		return result;
	}

	public static int GetValueAsInt(String b64Value) {
		int result = 0;
		if (!StringUtils.isEmpty(b64Value)) {
			byte[] values = Base64.getDecoder().decode(b64Value);

			if (values.length > 0) {
				result = 0;
				for (int i = 0; i < values.length; i++) {
					result = result << 8;
					result += values[i];
				}
			}
		}
		return result;
	}

	public static double GetValueAsDouble(String b64Value) {
		double result = 0;
		if (!StringUtils.isEmpty(b64Value)) {
			byte[] values = Base64.getDecoder().decode(b64Value);
			result = BitConverter.ToDouble(values, 0);
		}
		return result;
	}

	public static String GetValueAsString(String b64Value) {
		String result = null;
		if (!StringUtils.isEmpty(b64Value)) {
			byte[] values = Base64.getDecoder().decode(b64Value);
			result = null;
			for (int i = 0; i < values.length; i++) {
				result += (char) values[i];
			}
		}
		return result;
	}

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

*/}
